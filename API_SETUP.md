# クラウドストレージAPI設定ガイド

このガイドでは、Google DriveとMicrosoft OneDrive/SharePointへの直接アップロード機能を有効にするための設定方法を説明します。

## 🔐 セキュリティに関する重要な注意事項

**本番環境では、絶対にクライアントサイドにAPIキーやシークレットを埋め込まないでください！**

推奨される方法:
1. バックエンドサーバーを用意し、そこでOAuth処理を行う
2. 環境変数を使用する
3. Firebase Functions や AWS Lambda などのサーバーレス関数を使用する

## 📝 Google Drive API設定

### 1. Google Cloud Consoleでプロジェクトを作成

1. [Google Cloud Console](https://console.cloud.google.com/)にアクセス
2. 新しいプロジェクトを作成または既存のプロジェクトを選択
3. 「APIとサービス」→「ライブラリ」に移動
4. 「Google Drive API」を検索して有効化

### 2. OAuth 2.0 クライアントIDを作成

1. 「APIとサービス」→「認証情報」に移動
2. 「認証情報を作成」→「OAuth クライアントID」を選択
3. アプリケーションの種類: 「ウェブアプリケーション」を選択
4. 名前: 「Roster Share Web App」
5. 承認済みのJavaScript生成元:
   ```
   http://localhost:8080
   https://your-domain.com
   ```
6. 承認済みのリダイレクトURI:
   ```
   http://localhost/auth
   https://localhost/auth
   ```
7. 「作成」をクリック

### 3. クライアントIDとシークレットを取得

作成後、以下の情報が表示されます:
- **クライアントID**: `YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com`
- **クライアントシークレット**: `YOUR_GOOGLE_CLIENT_SECRET`

### 4. コードに設定

`lib/services/cloud_storage_service.dart`を開いて、以下の値を更新:

```dart
static const String _googleClientId = 'YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com';
static const String _googleClientSecret = 'YOUR_GOOGLE_CLIENT_SECRET';
```

---

## 📝 Microsoft Graph API設定（OneDrive/SharePoint）

### 1. Azure ポータルでアプリを登録

1. [Azure Portal](https://portal.azure.com/)にアクセス
2. 「Microsoft Entra ID」（旧Azure Active Directory）に移動
3. 「アプリの登録」→「新規登録」をクリック

### 2. アプリケーション設定

1. 名前: 「Roster Share」
2. サポートされているアカウントの種類: 「任意の組織ディレクトリ内のアカウントと個人のMicrosoftアカウント」
3. リダイレクトURI:
   - プラットフォーム: Web
   - URI: `https://localhost/auth`
4. 「登録」をクリック

### 3. API アクセス許可を追加

1. 左メニューの「APIのアクセス許可」をクリック
2. 「アクセス許可の追加」をクリック
3. 「Microsoft Graph」を選択
4. 「委任されたアクセス許可」を選択
5. 以下のアクセス許可を追加:
   - `Files.ReadWrite`
   - `offline_access`
6. 「アクセス許可の追加」をクリック

### 4. クライアントシークレットを作成（オプション）

1. 左メニューの「証明書とシークレット」をクリック
2. 「新しいクライアントシークレット」をクリック
3. 説明を入力し、有効期限を選択
4. 「追加」をクリック
5. **シークレット値をコピー**（後で確認できません！）

### 5. アプリケーションIDを取得

1. 「概要」ページに戻る
2. **アプリケーション（クライアント）ID**をコピー

### 6. コードに設定

`lib/services/cloud_storage_service.dart`を開いて、以下の値を更新:

```dart
static const String _msClientId = 'YOUR_MS_CLIENT_ID';
```

---

## 🔄 リダイレクトURIの設定

### ローカル開発環境

開発時は`https://localhost/auth`を使用します。これは、Flutterアプリがローカルで実行されている場合のリダイレクトURIです。

### 本番環境

本番環境では、実際のドメインを使用してください:
```
https://your-domain.com/auth
```

両方のプラットフォーム（Google・Microsoft）で、本番環境のリダイレクトURIを追加してください。

---

## 🧪 テスト方法

### Google Drive API テスト

1. アプリを起動
2. イベントを作成
3. 共有メニューから「Google Drive（直接アップロード）」を選択
4. Google認証画面が表示される
5. アカウントを選択し、アクセス許可を承認
6. アップロード成功メッセージと共有リンクが表示される

### OneDrive API テスト

1. アプリを起動
2. イベントを作成
3. 共有メニューから「OneDrive（直接アップロード）」を選択
4. Microsoft認証画面が表示される
5. アカウントでサインインし、アクセス許可を承認
6. アップロード成功メッセージと共有リンクが表示される

---

## ⚠️ トラブルシューティング

### 「redirect_uri_mismatch」エラー

**原因**: リダイレクトURIが登録されていない

**解決策**:
1. Google Cloud ConsoleまたはAzure Portalに戻る
2. 使用しているリダイレクトURIが正確に登録されているか確認
3. `https://localhost/auth`が追加されているか確認

### 「invalid_client」エラー

**原因**: クライアントIDまたはシークレットが間違っている

**解決策**:
1. コード内の値を再確認
2. スペースや改行が含まれていないか確認
3. 正しい値をコピー＆ペースト

### 「insufficient_permissions」エラー

**原因**: 必要なAPIアクセス許可が付与されていない

**解決策**:
1. APIアクセス許可を確認
2. 必要な権限が追加されているか確認
3. 管理者の承認が必要な場合は承認を依頼

### CORS エラー（Webアプリの場合）

**原因**: クロスオリジンリクエストがブロックされている

**解決策**:
1. 承認済みのJavaScript生成元を確認
2. 開発環境のURLを追加
3. ブラウザのCORS拡張機能を使用（開発時のみ）

---

## 🔒 セキュリティのベストプラクティス

### 本番環境での推奨構成

#### Option 1: バックエンドサーバーを使用

```
Flutter App (フロントエンド)
    ↓ (1) アップロードリクエスト
バックエンドサーバー
    ↓ (2) OAuth処理 + ファイルアップロード
Google Drive / OneDrive
    ↓ (3) 共有リンク返却
バックエンドサーバー
    ↓ (4) 共有リンクを返す
Flutter App (フロントエンド)
```

**メリット**:
- クライアントサイドにシークレットを保存しない
- より安全な認証フロー
- トークンのリフレッシュ管理が容易

#### Option 2: Firebase Functions を使用

```dart
// Firebase Functionsで実装
exports.uploadToGoogleDrive = functions.https.onCall(async (data, context) => {
  // 認証確認
  if (!context.auth) {
    throw new functions.https.HttpsError('unauthenticated', 'User must be logged in');
  }
  
  // Google Drive APIにアップロード
  // ...
  
  return { shareLink: 'https://...' };
});
```

#### Option 3: 環境変数を使用

開発時:
```bash
export GOOGLE_CLIENT_ID="your_client_id"
export GOOGLE_CLIENT_SECRET="your_client_secret"
export MS_CLIENT_ID="your_ms_client_id"
```

コード内:
```dart
static final String _googleClientId = 
  const String.fromEnvironment('GOOGLE_CLIENT_ID');
```

---

## 📚 参考リンク

- [Google Drive API Documentation](https://developers.google.com/drive/api/v3/about-sdk)
- [Microsoft Graph API Documentation](https://learn.microsoft.com/en-us/graph/overview)
- [OAuth 2.0 for Web Apps](https://developers.google.com/identity/protocols/oauth2)
- [Flutter Web Auth Package](https://pub.dev/packages/flutter_web_auth)

---

## ⚡ 代替手段: 手動アップロード

API設定が複雑な場合、**手動アップロード機能**も引き続き使用できます:

1. 共有メニューから「Google Drive（手動）」を選択
2. CSVファイルがダウンロードされる
3. Google Driveが自動的に開く
4. 手動でファイルをアップロード

この方法なら、API設定なしで使用できます！

---

**注意**: このガイドは開発・テスト用です。本番環境では、必ずバックエンドサーバーを経由してクラウドストレージAPIを使用してください。
